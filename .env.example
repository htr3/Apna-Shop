# Example local Postgres connection
# Create a DB and replace user/password/dbname as needed
DATABASE_URL=postgres://postgres:password@127.0.0.1:5432/shopkeeper_insights
# Optional: port the server should listen on
PORT=5000
